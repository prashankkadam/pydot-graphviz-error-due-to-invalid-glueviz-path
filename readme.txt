The code in the envs.py file can be used to set the environment variable path during the runtime 
Make sure you download and install graphviz-2.38.msi and install it in an appropriate directory
use the code in envs.py file and set the path to the bin folder of the installed directory
